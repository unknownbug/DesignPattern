## 抽象工厂模式
工厂模式是定义一个用于创建对象的接口，让子类决定来实例化哪一个类。
业务逻辑与数据访问解耦

抽象工厂模式：提供一个创建一系列相关或相互依赖对象的接口，而无需指定他们具体的类

反射：程序在运行时动态获取对象属性与方法的一种机制，即编译器需要将类型信息（属性类型与偏移地址以及成员函数的地址等信息）编译到程序文件中，当程序运行时将这些信息加载到内存中，做到运行时只根据对象的地址或引用就可以获取到对象的类型信息，从而用这些信息达到修改或重建对象的目标。

